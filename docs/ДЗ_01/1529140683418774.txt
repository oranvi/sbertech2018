—проектировать дизайн соц. сети. ¬ данном задании интересует разбитие приложени€ на модули, взаимодействие интерфейсов, а не реализаци€ конкретных классов.

—оц. сети обычно предлагают большой набор сервисов: 
- поиск/добавление друзей, 
- просмотр профилей,  
- загрузка и просмотр фото, 
- общение через чат или стены, 
- рекомендации, 
- подарки
- и куча других сервисов.

¬се эти сервисы должны находитс€ в отдельных модул€х и иметь св€зи между собой.
¬аша задача создать maven проект, создать модули дл€ каждого сервиса, прописать зависимости одних модулей от других. ¬ каждом модуле должны быть интерфейсы и доменна€ модель данного сервиса + в некоторых модул€х нужна примерна€ реализаци€ интерфейсов, где показано как используютс€ интерфейсы других модулей.


Ќеобходимо разбитьс€ на пары: первый разработчик создает репозиторий и пустой парент модуль в нем.
¬торой разработчик в паре создает fork в своем репозитории.
ƒалее каждый создает в своем github репозиторие 3-4 дочерних модул€ с сервисами.
«атем каждый делает запрос на сли€ние друг другу, в итоге у каждого получаетс€ по 6-8 сервисов.